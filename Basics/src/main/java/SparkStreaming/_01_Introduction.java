package SparkStreaming;

public class _01_Introduction {
    /**
     *
     * Introduction to Spark Streaming
     *      - Working with live streams of data
     *      - Analyse Page views, movements of mouse pointer, live clicks
     *      - Building a live dashboard and their usage
     *      - Analyse service logs in real time.
     *      - Live analysis for share price updates
     *
     * Features
     *      - Ease of use
     *      - Writing similar to batch jobs.
     *      - Fault tolerance (nodes can recover and resume)
     *
     * We have 2 flavour of streaming (both versions are entirely different)
     *      - DStreams - traditional model built on RDD API. (no future developments)
     *      - Structures Streaming - new model based on SparkSQL engine (we can use dataframes API). It doesn't contain DStreams under the hood
     *
     * */
    @SuppressWarnings("resource")
    public static void main(String[] args) throws InterruptedException {

    }
}
